# CURRENT.md — you are here

Single-page status pointer. Updated each session.

## Active milestone

**M0 — initial scaffold + first screen wired to real data**

Goal: from "empty repo" to "app launches on iPhone 17 Pro showing a Today screen with sample tasks read from disk."

## Last session

_(see `session_logs/` for chronological history)_

## What exists right now

- Repo skeleton (root files, `.gitignore`, `LICENSE`, `README.md`, `CLAUDE.md`, `PRODUCT-SPEC.md`)
- Cross-platform foundations brought across from `/Users/saxon/Developer/Projects/archive/lists`:
  - `research/` — multi-platform plan + per-platform stack docs
  - `shared/` — language-neutral file format spec, fixtures, lexicons, etc.
  - `platforms/{android,linux,windows}/` — placeholder structure for later milestones
- iOS Xcode project (`platforms/ios/Lists.xcodeproj`) generated by XcodeGen
- Empty SwiftUI app + design tokens (sage / warm-neutral / SF Pro Rounded)
- Item data model + file storage (markdown + YAML frontmatter) + tests
- Today smart list view wired to `ItemStore`
- First-launch sample data (3 tasks)

## What's next

In rough priority order:

1. **Tier 1 design mockups for the remaining screens** — Sidebar/Home, single list (vertical + column), item detail sheet. Run another design session to produce these.
2. **Sidebar + Home view** — currently the app jumps straight into Today. Needs the navigation root.
3. **Item detail sheet** — read + edit. Currently you can only toggle done.
4. **Quick capture flow** — floating "+" button, capture sheet.
5. **Settings** — per design Tier 3.
6. **Notification scheduling** (regular `UNUserNotificationCenter`; AlarmKit is M-later, gated on paid Developer Program).

## Known follow-ups + open questions

- **`shared/format/` uses old "reminder" naming** — needs realignment with the new "item" primitive (single type with `task | habit | note` discriminator). Not blocking; do this when next touching the format spec.
- **Tier 1 screen mockups are not yet exported as PNGs.** The prototype HTML is the visual reference; if a per-screen image library would be useful, run that pass during the next design session.
- **`shared/lexicons/shopping.en.json`** is a 20-entry skeleton. Needs ~200 entries before grocery mode ships.
- **License consistency** — `LICENSE` is AGPL-3.0-or-later; older archive used "or-only". Spec, README, and LICENSE all aligned now.
- **Distribution exception drafting** — App Store submission under AGPL needs a `LICENSE.app-store-exception` (per spec §17). Not needed until App Store is in scope.

## Don't forget

- Bundle id `io.github.saxonbobart.lists`, team `899XX9P8T4` (Personal — AlarmKit deferred)
- Drive the simulator before claiming iOS work done
- Conventional Commits, never push without explicit go
