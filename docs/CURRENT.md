# CURRENT.md — you are here

Single-page status pointer. Updated each session.

## Active milestone

**M0 — initial scaffold + Today screen wired to real data ✅ DONE**

The app launches on iPhone 17 Pro, shows a Today screen with 3 sample tasks (one overdue, one today, one tomorrow), reads them from disk, and persists across restarts. Files on disk match the spec format exactly (markdown + YAML frontmatter, human-readable in any text editor).

## What's next

Pick the next milestone — recommendation in priority order:

1. **Tier 1 design mockups for the remaining screens** — Sidebar / Home, single list (vertical + column), item detail sheet, quick capture. Run another design session in `design/` to produce these. The current bundle has them as JSX prototype but we need either pixel-perfect SwiftUI translations or PNG reference images.
2. **Sidebar + Home view** — currently the app jumps straight into Today. Needs the navigation root (pinned smart-list tiles + lists section + tags / recently-deleted).
3. **Item detail sheet** — read + edit. Currently you can only toggle done. Tapping a row should open a sheet with title, body, date+time, reminder toggle, tags, priority, flag.
4. **Quick capture flow** — floating "+" button (currently disabled), capture sheet, list picker.
5. **Sub-items + thread view** — schema supports `parent` already; needs UI for nesting + the flattened H1/H2/H3 thread editor.
6. **Habit detail + heatmap** — model is in place; needs the visualisation (12-month grid, completion ring, edit history).
7. **Settings** — per design Tier 3 (Appearance, Sync, Triggers, Notifications, Data, About).
8. **Notification scheduling** — `UNUserNotificationCenter` for date-based items. **AlarmKit (urgent triggers) is M-later**, gated on a paid Apple Developer Program.

## What exists right now

- Repo skeleton: `.gitignore`, `LICENSE` (AGPL-3.0-or-later), `README.md`, `CLAUDE.md`, `PRODUCT-SPEC.md` (v2 spec, 1511 lines)
- Cross-platform foundations from `/Users/saxon/Developer/Projects/archive/lists`:
  - `research/` — multi-platform plan + per-platform stack docs (Android / Linux / Windows)
  - `shared/` — language-neutral file format spec, fixtures, lexicons, etc.
  - `platforms/{android,linux,windows}/` — placeholder structure for later milestones
- iOS Xcode project (`platforms/ios/Lists.xcodeproj`) generated by XcodeGen
  - SwiftUI app, Swift 6.2, iOS 26.0, scheme `Lists`, bundle `io.github.saxonbobart.lists`, team `899XX9P8T4`
  - SPM packages: Yams (YAML), MarkdownUI (markdown rendering, future)
- Design tokens (`Lists/Design/`) — sage on warm-neutral, SF Pro Rounded, light + dark, derived from `design/Claude Design/project/tokens.css`
- Data layer (`Lists/Core/`):
  - `Item` primitive (task / habit / note) with custom Codable + ISO 8601 dates
  - `ItemList` metadata
  - `FrontmatterCodec` (markdown + YAML round-trip)
  - `FileStore` actor (read/write/delete to `Documents/Lists/<listId>/...`)
  - `ItemStore` (`@Observable @MainActor` coordinator)
  - `SmartList` predicates (today / scheduled / all / completed / flagged / urgent)
  - `SampleData` (3 first-launch tasks)
- Today smart list view (`Lists/Features/Today/`) — sectioned overdue + today, ItemRow with checkbox / body preview / time / tag chips / flag, FAB stub
- Tests: 19 passing (Swift Testing) covering item round-trip, FileStore, smart list predicates, sample data + persistence

## Known follow-ups + open questions

- **`shared/format/` uses old "reminder" naming** — needs realignment with the new `item` primitive (single type with `task | habit | note` discriminator). Not blocking; do this when next touching the format spec.
- **Tier 1 screen mockups are not yet exported as PNGs.** The prototype HTML is the visual reference; if a per-screen image library would be useful, run that pass during the next design session.
- **`shared/lexicons/shopping.en.json`** is a 20-entry skeleton. Needs ~200 entries before grocery-mode ships.
- **Distribution exception drafting** — App Store submission under AGPL needs a `LICENSE.app-store-exception` (per spec §17). Not needed until App Store is in scope (and that's gated on the paid Developer Program).
- **Old simulator data conflict** — the Simulator's container at `~/Library/Developer/CoreSimulator/Devices/65D9F3B1.../Data/Application/.../Documents/Lists/` had leftover `.list.yml` files from the archive build using a different schema (`kind` instead of `default_item_type`, `created` instead of `created_at`). The new app errors when it encounters these. If you switch sims or this happens again, wipe `Documents/Lists/` for the bundle. Long-term fix: a `data_version` field + migration shim.
- **No `tap` UI automation** in the current XcodeBuildMCP install — manual checkbox interaction not scripted from an MCP tool. The toggle+persist path is covered by the `toggleDonePersists` test; manual visual confirmation requires opening the simulator window.

## Don't forget

- Bundle id `io.github.saxonbobart.lists`, team `899XX9P8T4` (Personal — AlarmKit deferred until paid)
- Drive the simulator (build_run_sim + screenshot) before claiming iOS work done
- Conventional Commits, never push without explicit go
- Saxon is non-technical — frame technical decisions as product effects
