<!-- placeholder — see ../../README.md -->
<!-- Shared domain logic (parser, cache, recurrence, RRULE expander, ULID).
     Pure .NET; no Avalonia dependency. Reusable from a future macOS/Linux/web
     C# build if Avalonia is ever extended that way. -->
<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <TargetFramework>net9.0</TargetFramework>
        <RootNamespace>Lists.Core</RootNamespace>
    </PropertyGroup>

    <ItemGroup>
        <!-- Wire once the build exists:
        <PackageReference Include="Microsoft.Data.Sqlite" Version="9.*" />
        <PackageReference Include="YamlDotNet" Version="16.*" />
        <PackageReference Include="Markdig" Version="0.*" />
        -->
    </ItemGroup>
</Project>
