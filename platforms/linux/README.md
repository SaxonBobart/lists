# Linux

Deferred.

Do not start Linux work unless Saxon explicitly asks for it. The iOS app in `platforms/ios/` is the active implementation.

When Linux becomes active, choose the native stack deliberately at that time and keep file-format compatibility with `shared/`.
