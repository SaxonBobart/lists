# placeholder — see ../README.md
# test_env = environment()
# test_env.set('G_MESSAGES_DEBUG', 'all')
#
# foreach test_name : ['frontmatter', 'cache', 'rrule']
#     exe = executable(
#         'test-' + test_name,
#         files(test_name + '.vala'),
#         dependencies: [gtk_dep, adw_dep, sqlite_dep, libfyaml_dep],
#     )
#     test(test_name, exe, env: test_env)
# endforeach
