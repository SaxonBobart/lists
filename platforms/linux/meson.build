# placeholder — see ./README.md
project(
    'lists',
    'vala', 'c',
    version: '0.1.0',
    license: 'AGPL-3.0-or-later',
    meson_version: '>= 1.4.0',
    default_options: [
        'warning_level=2',
        'c_std=gnu11',
    ],
)

# App ID conventions used across data/, src/, flatpak/.
app_id = 'io.github.saxonbobart.Lists'
gettext_package = 'lists'

# Dependencies will be wired here once the build is real:
#   gtk_dep        = dependency('gtk4',          version: '>= 4.16')
#   adw_dep        = dependency('libadwaita-1',  version: '>= 1.7')
#   gtksv_dep      = dependency('gtksourceview-5')
#   webkit_dep     = dependency('webkitgtk-6.0')
#   sqlite_dep     = dependency('sqlite3')
#   libfyaml_dep   = dependency('libfyaml')
#   gst_dep        = dependency('gstreamer-1.0')

# Subdirectories — uncomment as each is implemented.
# subdir('po')
# subdir('data')
# subdir('src')
# subdir('tests')

# Run gnome.post_install() once data/ is wired.
