# placeholder — see ../README.md
# Sub-build for the main binary. Real configuration wired once the Vala
# sources exist.

# lists_sources = files(
#     'main.vala',
#     'application.vala',
#     'window.vala',
# )
#
# lists_resources = gnome.compile_resources(
#     'lists-resources',
#     'lists.gresource.xml',
#     dependencies: blueprints,
#     source_dir: meson.current_build_dir(),
# )
#
# executable(
#     'lists',
#     lists_sources,
#     lists_resources,
#     dependencies: [gtk_dep, adw_dep, gtksv_dep, webkit_dep, sqlite_dep, libfyaml_dep],
#     install: true,
# )
