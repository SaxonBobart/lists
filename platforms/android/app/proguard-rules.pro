# placeholder — see ../README.md
# Project-specific proguard rules. R8 in release minifies/shrinks; rules below
# preserve types touched only by reflection (snakeyaml-engine, Room entities,
# Hilt-generated classes). Real rules added once code exists.
