// Top-level build file. Plugin versions live in gradle/libs.versions.toml.
// Plugins are applied per-module; nothing is applied at the root so that
// :core stays buildable in environments without the Android SDK.
