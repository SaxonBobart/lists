# Android

Deferred.

Do not start Android work unless Saxon explicitly asks for it. The iOS app in `platforms/ios/` is the active implementation.

When Android becomes active, prefer a native Kotlin/Compose implementation that reads and writes the shared markdown/YAML format in `shared/`.
