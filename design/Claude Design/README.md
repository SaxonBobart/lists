# Claude Design Export

This is an exported design prototype. It is reference material only.

Primary file:

```text
project/Lists Design.html
```

Use the HTML, JSX, and CSS to understand visual intent, then implement with native SwiftUI patterns.
