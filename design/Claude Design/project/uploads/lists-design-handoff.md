# Lists Design Handoff

Historical handoff from the Claude Design prototype session.

Current use:

- The prototype in `../Lists Design.html` is the visual reference.
- The root `PRODUCT-SPEC.md` is the product behavior reference.
- `AGENTS.md` is the coding-agent workflow reference.

Do not copy prototype structure directly into the app. Recreate the intended native iOS experience in SwiftUI.
